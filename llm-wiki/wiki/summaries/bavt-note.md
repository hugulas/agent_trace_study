# Spend Less, Reason Better: Budget-Aware Value Tree Search for LLM Agents

## Source

- Raw note: `raw/notes/bavt_note.md`
- Metadata: not available in note

## Compiled Summary

测试时扩展已成为提升大语言模型智能体可靠性的主导范式，然而现有方法将计算资源视为无限充裕，导致智能体在冗余步骤或死胡同轨迹上耗尽令牌与工具预算。现有的预算感知方法要么需要昂贵的微调，要么依赖粗粒度的轨迹级启发式规则，无法在执行中途进行干预。本文提出预算感知价值树（BAVT），一种无需训练的推理时框架，它将多跳推理建模为由单一大语言模型骨干引导的动态搜索树，并在步级别进行价值估计。另一项关键创新是预算条件节点选择机制，该机制将剩余资源比例作为节点价值的自然缩放指数，随着预算消耗提供从广泛探索到贪婪利用的有原则、无参数过渡。为应对大语言模型自我评估中众所周知的过度自信问题，BAVT 采用残差价值预测器，对相对进展而非绝对状态质量进行评分

## Evidence Notes

- 当前大语言模型智能体在解决复杂多跳推理任务时，越来越依赖测试时扩展策略——通过增加推理时的计算资源来提升可靠性。然而，这些工作普遍存在两个根本缺陷：
- **资源管理缺失**：现有智能体通常在无限制或扩展预算的假设下追求准确率最大化，缺乏细粒度的预算控制机制。这导致它们频繁在冗余或低收益的步骤上耗尽令牌限额和工具调用配额。
- - **干预粒度粗糙**：既有预算感知方法仅在轨迹级别将剩余预算信息嵌入提示，依赖大语言模型的隐式自我调节能力。由于无法在中间推理步骤进行实时干预，这些框架根本不能检测并放弃失败的轨迹，导致智能体反复陷入死胡同或无限循环，在无望方向上默默消耗大量预算。
- ### 问题形式化
论文将预算感知智能体推理形式化为资源受限的确定性决策过程，定义为五元组：
$$
(S, A, T, B, C)
$$
其中各分量含义如下：
- **状态空间**：状态包含初始用户查询、历史动作、内部推理轨迹和外部环境返回的观察结果
- **动作空间**：动作涵盖内部推理生成和外部工具调用
- **转移动态**：环境根据当前状态和动作确定性转移到下一状态，将新动作及其观察追加到上下文中
- **预算状态空间**：预算不是静态约束，而是动态追踪的二维状态空间，分别记录剩余工具调用次数和剩余输出令牌数
- **成本函数**：每个动作产生特定计算和财务成本，其中工具调用成功时成本为一，否则为零；令牌成本对应该步模型生成的
- ### 机制流程
> [!figure] Fig.
- ### 主要性能对比
在四个多跳问答基准上，BAVT 在所有预算层级和数据集上均严格优于并行采样基线。最显著的发现是：**BAVT 在严格的低预算约束（五次工具调用）下，其性能始终匹敌或超过基线在高预算配置（二十次调用）下的表现**，证明智能资源管理从根本上优于四倍暴力计算扩展。
- 在 OSS-20B 专用推理模型上，低预算层级的平均 EM 从基线的零点一九四提升至 BAVT 的零点三三八。Bamboogle 数据集上提升尤为显著：低预算下 EM 从零点三一提升至零点四九。

## Wiki Connections

- Concepts: [runtime-instrumentation-and-otel](concepts/runtime-instrumentation-and-otel.md), [evaluation-and-benchmarking](concepts/evaluation-and-benchmarking.md), [cost-token-and-resource-attribution](concepts/cost-token-and-resource-attribution.md)
- Entities: None identified
